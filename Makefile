install: 
	instcomp --install feedrate.c
	instcomp --install solo-estop.c
	instcomp --install torque.c
	instcomp --install feedrate-v2.c
	instcomp --install probe-error.c
	instcomp --install reset-pin.c
